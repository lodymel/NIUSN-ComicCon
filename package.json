{
  "name": "niusn-landing",
  "private": true,
  "scripts": {
    "dev": "npx --yes serve .",
    "build": "npm run build:map",
    "build:map": "cd uploads/niusn-map && npm install && npm run build"
  }
}
